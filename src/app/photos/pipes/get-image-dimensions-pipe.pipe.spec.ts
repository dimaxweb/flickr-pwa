import { GetImageDimensionsPipePipe } from './get-image-dimensions-pipe.pipe';

describe('GetImageDimensionsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GetImageDimensionsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
